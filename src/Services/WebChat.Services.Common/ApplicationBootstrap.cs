namespace WebChat.Services.Common
{
    #region Using

    using Ninject;
    using WebChat.Services.Interfaces;

    #endregion
    public static partial class ApplicationBootstrap
    {
        //private static readonly object Mutex = new object();

        //private static IDependencyContainer container = default(IDependencyContainer);

        //public static IDependencyContainer GetModelContainer()
        //{
        //    lock (Mutex)
        //    {
        //        if (container == null)
        //        {
        //            container = new NinjectDependencyResolver(new StandardKernel());
        //        }

        //        return container;
        //    }
        //}
    }
}
